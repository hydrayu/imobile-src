log box 可以拿來當命令的介面，方法如下:

1. 快速 click log box, 直到它由灰變白
2. 每次下命令前，請把 log box 的訊息清光，使用全選+Backspace
3. 提供的命令如下

jpg : 將照相切成 jpg 格式，此為預設模式
bmp : 將照相切成 bmp 格式
png : 將照相切成 png 格式

=== : 將預覽(錄影)解析度設成與所選的解析度一致
      記得要重新切換解析度才會生效
!!! : 將預覽(錄影)最大解析度設成 1024x768
      記得要重新切換解析度才會生效
log : 打開 log 模式，此為預設模式
nolog: 關閉 log 模式
